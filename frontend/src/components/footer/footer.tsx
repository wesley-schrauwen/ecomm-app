import styled from 'styled-components';

const FooterStyle = styled.div`
  background: aliceblue;
  height: 128px;
  margin-bottom: 0;
  margin-top: auto;
`

export const Footer = () => {
  return <FooterStyle />;
}